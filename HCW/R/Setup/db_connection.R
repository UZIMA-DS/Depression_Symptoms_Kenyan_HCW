con <- dbConnect(odbc(),
                 Driver = #Enter Driver,
                 Server = #Enter server name,
                 Database = #Enter Database name
                 username = '',
                 Authentication = 'ActiveDirectoryInteractive')
  